version: 1
piri:
  nodes:
    - storage:
        db: postgres # or 'sqlite'
        blob: s3 # or 'filesystem'
    # supports a total of 9 piri nodes, 9 due to anvil wallet limit
    #- storage:
    #    db: sqlite
    #    blob: filesystem
    #- storage:
    #    db: postgres
    #    blob: s3
